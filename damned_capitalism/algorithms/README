1.  interface:
        1)directory that include:
           -configuration.json
           -source files of your algorithm
        2)input of your algorithms:
           -t : time for calculation
           -d : database name
           -u : user login
           -p : password

        3)output of your algorithms:
            answer.json file which contains indexes of items
2.  check simple_random dir as an example